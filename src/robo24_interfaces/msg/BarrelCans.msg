# Barrel race message
# Up to 3 can detections distance (meters) and angle (radians)
# No can detected when distance = 0.0

float64 can1_distance
float64 can1_angle    

float64 can2_distance 
float64 can2_angle    

float64 can3_distance 
float64 can3_angle    
